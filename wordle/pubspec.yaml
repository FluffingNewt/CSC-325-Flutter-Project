name: wordle
description: "Flutte version of the popular game Wordle!"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0-157.0.dev <4.0.0'

dependencies:
  equatable: ^2.0.3
  flip_card: ^0.6.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
